package withbreeze
import breeze.linalg._
import breeze.linalg.operators
import breeze.numerics._
object breeze {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(157); 
  println("Welcome to the Scala worksheet");$skip(25); 
  
  val a = List(1,2,3);System.out.println("""a  : List[Int] = """ + $show(a ))}
  
  
  
}
